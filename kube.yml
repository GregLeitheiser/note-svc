apiVersion: apps/v1
kind: Deployment
metadata:
  name: note-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: note-svc
  template:
    metadata:
      labels:
        app: note-svc
    spec:
      containers:
      - name: note-svc
        image: servantscode/note-svc
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "200Mi"
        env:
        - name: ES_JAVA_OPTS 
          value: "-Xmx128m -Xms128m"
---
kind: Service
apiVersion: v1
metadata:
  name: note-svc
spec:
  selector:
    app: note-svc
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
